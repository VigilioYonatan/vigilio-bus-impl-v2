name: bus-impl-v2-ci

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  merge_group:
    types: [checks_requested]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  actions: read
  contents: read
  packages: read
  attestations: write
  id-token: write
  security-events: write
  pull-requests: read

jobs:
  application:
    uses: VigilioYonatan/vigilio-platform-actions/.github/workflows/shared-backend-node-nest-ci.yml@a46b75d3bdac70b49596c7dbd8aebbbc3f6bb3ab # v1
    with:
      has-cdk: false
      has-openapi: true
      has-postgres: true
      run-aws-integration: true
      run-bdd: true
      run-e2e: true
      run-security-deep: true
      e2e-script: test:e2e
      attest-artifact: true
      gitops-bot-actor: github-actions[bot]
    secrets: inherit

